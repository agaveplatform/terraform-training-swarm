Adjust vars/main.yml to have appropriate variables.  

Run `vagrant up` or any other provision method choose to use run ansible.

Designed and tested on ubuntu, may work on debian, will not work on rhel/centos